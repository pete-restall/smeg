[package]
name = "smeg-testing-integration"
version = "0.1.0"
edition = "2024"
authors = ["Pete Restall <pete@restall.net>"]

[dependencies]
fluent-test = "0.4.3"
smeg-kernel = { path = "../../kernel", features = ["no_default_despair"] }
smeg-testing-host-utils = { path = "../host-utils" }
